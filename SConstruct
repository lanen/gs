
Program('build/main',Glob('src/*.c'),srcdir='src',
        
         LIBS = ['zlog','pthread'],
         INCLUDEPATH='usr/local/include',
         LIBPATH = ['/usr/lib','/usr/local/lib'],
        parse_flags = '-I/usr/local/include'        
        )
